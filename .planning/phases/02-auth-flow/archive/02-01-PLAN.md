---
phase: 02-auth-flow
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tools/hlx-admin-mcp/test-phase2.sh
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-05
  - DA-05

must_haves:
  truths:
    - "test-phase2.sh exists and is executable"
    - "Script checks AUTH-01: unauthenticated POST /mcp returns 401"
    - "Script checks AUTH-02: GET /login?session=<id> returns 302"
    - "Script checks AUTH-05: no 'localhost:3000' in src/http.ts"
    - "Script checks DA-05: POST /mcp da_login in httpMode returns a URL string"
    - "Running the script before implementation produces FAIL output (tests are real, not stubs)"
  artifacts:
    - path: "tools/hlx-admin-mcp/test-phase2.sh"
      provides: "Phase 2 smoke test suite"
      contains: "AUTH-01, AUTH-02, AUTH-05, DA-05 checks"
  key_links:
    - from: "test-phase2.sh"
      to: "tools/hlx-admin-mcp/dist/http.js"
      via: "PORT=9999 node dist/http.js background process"
      pattern: "PORT=9999 node dist/http.js"
---

<objective>
Create the test-phase2.sh smoke test script that validates all automatable Phase 2 requirements.

Purpose: Wave 0 test infrastructure — every subsequent plan's verify step runs this script. Must exist and FAIL before implementation so the Red phase is explicit.
Output: tools/hlx-admin-mcp/test-phase2.sh
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-auth-flow/02-VALIDATION.md

# Pattern reference — follow this structure exactly
@tools/hlx-admin-mcp/test-phase1.sh
</context>

<interfaces>
<!-- Key types and patterns from existing test-phase1.sh -->
<!-- test-phase2.sh must match this structure: set -e, PASS/FAIL counters, cleanup trap, PORT=9999 server start, sleep 2, curl checks, Results summary, exit 1 on FAIL -->

From test-phase1.sh (structure to replicate):
```bash
#!/usr/bin/env bash
set -e
PASS=0; FAIL=0; SERVER_PID=""
pass() { echo "[PASS] $1"; PASS=$((PASS+1)); }
fail() { echo "[FAIL] $1"; FAIL=$((FAIL+1)); }
cleanup() { if [ -n "$SERVER_PID" ]; then kill "$SERVER_PID" 2>/dev/null || true; wait "$SERVER_PID" 2>/dev/null || true; SERVER_PID=""; fi }
trap cleanup EXIT
# PORT=9999 node dist/http.js &; SERVER_PID=$!; sleep 2
# curl checks
echo "=== Results: $PASS passed, $FAIL failed ==="; if [ "$FAIL" -gt 0 ]; then exit 1; fi
```

From http.ts (relevant paths and response shapes):
- POST /mcp with no Authorization header → currently passes through (Phase 2 will make it return 401)
- GET /login?session=<id> → route does not exist yet (will return 302 after Phase 2)
- Static: `grep -r 'localhost:3000' src/http.ts` must return empty after Phase 2
- POST /mcp with da_login tool call → currently opens browser; after Phase 2 returns URL string
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create test-phase2.sh smoke test script</name>
  <files>tools/hlx-admin-mcp/test-phase2.sh</files>
  <action>
Create tools/hlx-admin-mcp/test-phase2.sh following the exact structure of test-phase1.sh. The script must:

1. Use `#!/usr/bin/env bash`, `set -e` (but override with || for curl failures), PASS/FAIL counters, cleanup trap on EXIT.
2. Run `npm run build 2>/dev/null || true` at the top (rebuild before smoke checks, non-fatal if already built).
3. Start server: `PORT=9999 ADOBE_IMS_CLIENT_ID=test-client node dist/http.js &` with `sleep 2` wait. Set ADOBE_IMS_CLIENT_ID so IMS_OAUTH_ENABLED=true (required for auth guard to activate).

Static checks (before server start, labeled "Phase 2 Static Checks"):

- **AUTH-05 static:** `grep -rn 'localhost:3000' src/http.ts` returns empty → pass "AUTH-05: no hardcoded localhost:3000 in http.ts"

Smoke checks (after server starts on PORT=9999, labeled "Phase 2 Smoke Checks"):

- **AUTH-01 status:** `curl -s -o /dev/null -w "%{http_code}" -X POST http://localhost:9999/mcp -H "Content-Type: application/json" -d '{"jsonrpc":"2.0","id":1,"method":"tools/list"}'` → expect "401" → pass "AUTH-01: unauthenticated POST /mcp returns 401"
- **AUTH-01 body login_url:** Same curl with `-o body.tmp`, then `grep -q '"login_url"' body.tmp` → pass "AUTH-01: 401 response body contains login_url field"
- **AUTH-01 WWW-Authenticate header:** `curl -sI -X POST ... | grep -i 'www-authenticate'` → pass "AUTH-01: 401 response includes WWW-Authenticate header"
- **AUTH-02 status:** `curl -s -o /dev/null -w "%{http_code}" "http://localhost:9999/login?session=test-uuid-1234"` → expect "302" → pass "AUTH-02: GET /login?session=<id> returns 302 redirect"
- **AUTH-02 location:** `curl -sI "http://localhost:9999/login?session=test-uuid-1234" | grep -i '^location:' | grep -q 'ims-na1.adobelogin.com'` → pass "AUTH-02: /login redirect points to Adobe IMS"
- **DA-05 url in response:** POST /mcp with da_login tool call while unauthenticated returns 401 (handled by AUTH-01 guard). In httpMode after auth, da_login should return URL. Test this by: inject a test Bearer → POST /mcp with tools/call da_login → response text must NOT contain "Browser opened" and must contain a URL. Since Bearer injection requires a real session, test the simpler check: `curl -s -X POST http://localhost:9999/mcp -H "Content-Type: application/json" -H "Authorization: Bearer nonexistent-session" -d '{"jsonrpc":"2.0","id":1,"method":"tools/call","params":{"name":"da_login","arguments":{"org":"test","site":"test"}}}' | grep -v 'Browser opened'` passes as long as the response doesn't say "Browser opened". Accept this as a weak check and note it in comments — the real test of DA-05 is manual.

NOTE: Write DA-05 check as: call da_login with a fake bearer → the 401 handling will kick in before da_login is ever called in hosted mode, so the actual da_login httpMode behavior is tested implicitly by manual verification. Just add a comment in the script: `# DA-05: da_login httpMode — tested manually (requires auth); automated check skipped`.

4. Add cleanup call to remove body.tmp temp file in the cleanup() function.
5. End with `=== Results: $PASS passed, $FAIL failed ===` and `exit 1` if FAIL > 0.
6. After creating the file, run `chmod +x tools/hlx-admin-mcp/test-phase2.sh`.

IMPORTANT: The AUTH-05 static check will FAIL before Phase 2 implementation (localhost:3000 still present). The AUTH-01 and AUTH-02 smoke checks will FAIL (routes don't exist yet). This is expected — Wave 0 creates the test harness, not the implementation.
  </action>
  <verify>
    <automated>cd C:/Users/remekie/Documents/Antigravity/tools/hlx-admin-mcp && test -f test-phase2.sh && echo "File exists" && head -5 test-phase2.sh</automated>
  </verify>
  <done>test-phase2.sh exists in tools/hlx-admin-mcp/, is executable, follows test-phase1.sh structure, and contains checks for AUTH-01, AUTH-02, AUTH-05 (static), and a DA-05 comment. Script runs and produces FAIL output against unmodified codebase (not zero FAILs).</done>
</task>

</tasks>

<verification>
Run from tools/hlx-admin-mcp/:
```
bash test-phase2.sh
```
Expected: script runs, AUTH-05 static check FAILs (localhost:3000 present), AUTH-01/AUTH-02 FAILs (routes not yet added). Script exits with code 1. This proves the test harness is real and not vacuously passing.
</verification>

<success_criteria>
- test-phase2.sh created and executable
- Script structure matches test-phase1.sh pattern (PASS/FAIL counters, cleanup trap, PORT=9999)
- AUTH-05 static grep check present
- AUTH-01 checks: HTTP 401 status, login_url in body, WWW-Authenticate header
- AUTH-02 checks: HTTP 302 status, Location header pointing to Adobe IMS
- Running against unmodified codebase produces failures (confirming tests are real)
</success_criteria>

<output>
After completion, create `.planning/phases/02-auth-flow/02-01-SUMMARY.md`
</output>
