---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - tools/hlx-admin-mcp/package.json
  - tools/hlx-admin-mcp/package-lock.json
autonomous: true
requirements:
  - HOST-02
  - HOST-03
  - HOST-04

must_haves:
  truths:
    - "selfsigned does not appear in package.json dependencies"
    - "npm ci installs cleanly without selfsigned"
    - "npm run build exits 0 on a fresh install"
    - "test-phase1.sh runs and all smoke checks pass"
  artifacts:
    - path: "tools/hlx-admin-mcp/package.json"
      provides: "Clean production dependencies without selfsigned"
      contains: "express"
    - path: "tools/hlx-admin-mcp/package-lock.json"
      provides: "Updated lockfile with selfsigned removed"
  key_links:
    - from: "package.json dependencies"
      to: "Railway npm ci build step"
      via: "npm uninstall selfsigned updates both files"
      pattern: "\"selfsigned\""
---

<objective>
Remove the selfsigned package from production dependencies and run the full phase test suite to confirm all four requirements are met.

Purpose: After Plan 01-01 removes the selfsigned import from http.ts, the package still exists in package.json dependencies and gets installed on every `npm ci` in Railway's build. This plan removes it cleanly and verifies the complete Phase 1 success criteria by running test-phase1.sh.

Output: package.json and package-lock.json with selfsigned removed, and a passing test-phase1.sh run confirming all HOST-02 through HOST-05 checks are green.
</objective>

<execution_context>
@c:/Users/remekie/Documents/Antigravity/.claude/get-shit-done/workflows/execute-plan.md
@c:/Users/remekie/Documents/Antigravity/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@c:/Users/remekie/Documents/Antigravity/.planning/PROJECT.md
@c:/Users/remekie/Documents/Antigravity/.planning/ROADMAP.md
@c:/Users/remekie/Documents/Antigravity/.planning/STATE.md

@c:/Users/remekie/Documents/Antigravity/.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Uninstall selfsigned package and verify build</name>
  <files>tools/hlx-admin-mcp/package.json, tools/hlx-admin-mcp/package-lock.json</files>
  <action>
    Run the following commands from the tools/hlx-admin-mcp/ directory:

    1. Uninstall selfsigned (updates package.json and package-lock.json):
       ```
       cd tools/hlx-admin-mcp
       npm uninstall selfsigned
       ```

    2. Verify the package is gone from package.json:
       ```
       node -e "const p=require('./package.json'); if(p.dependencies['selfsigned']) { console.error('FAIL: selfsigned still present'); process.exit(1); } console.log('OK: selfsigned removed');"
       ```

    3. Run the TypeScript build to confirm the file compiles without selfsigned:
       ```
       npm run build
       ```
       Build must exit 0. If it fails, the selfsigned import was not removed in Plan 01-01.
       Check 01-01-SUMMARY.md for what was actually done, then fix any remaining import.

    4. Confirm dist/http.js was produced:
       ```
       test -f dist/http.js && echo "dist/http.js exists"
       ```
  </action>
  <verify>
    <automated>cd "c:/Users/remekie/Documents/Antigravity/tools/hlx-admin-mcp" && node -e "const p=require('./package.json'); process.exit(p.dependencies['selfsigned'] ? 1 : 0)" && npm run build 2>&1 | tail -3 && test -f dist/http.js && echo "ALL OK"</automated>
  </verify>
  <done>
    - selfsigned not in package.json dependencies
    - npm run build exits 0
    - dist/http.js exists
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full phase test suite and verify all checks pass</name>
  <files>tools/hlx-admin-mcp/test-phase1.sh</files>
  <action>
    Run the complete phase test script to confirm all HOST-02 through HOST-05 requirements
    are satisfied:

    ```
    cd tools/hlx-admin-mcp
    bash test-phase1.sh
    ```

    Expected output: 10 PASS lines and "Results: 10 passed, 0 failed"

    If any check fails:
    - HOST-03 selfsigned static check fails → Task 1 of this plan did not complete correctly
    - HOST-05 railway.toml checks fail → Plan 01-02 was not completed; verify that plan ran first
    - HOST-04 /health HTTP 200 fails → Check dist/http.js was built from the corrected http.ts
    - HOST-02 0.0.0.0 static check fails → Plan 01-01 Task 3 bind address change was not saved
    - HOST-03 port 3443 check fails → oauthApp removal in Plan 01-01 was incomplete

    For each failure, consult the relevant SUMMARY.md file and fix the underlying issue before
    re-running the test suite. Do not mark this task done until test-phase1.sh exits 0.

    Note: On Windows with Git Bash or WSL, `chmod +x` may need to be run first:
    `chmod +x test-phase1.sh`
    If bash is not available, run the individual checks manually as listed in VALIDATION.md.
  </action>
  <verify>
    <automated>cd "c:/Users/remekie/Documents/Antigravity/tools/hlx-admin-mcp" && bash test-phase1.sh 2>&1</automated>
  </verify>
  <done>
    test-phase1.sh exits 0. Output shows "Results: 10 passed, 0 failed" (or at minimum all
    HOST-02, HOST-03, HOST-04, HOST-05 checks are green).
  </done>
</task>

</tasks>

<verification>
Full phase gate — all four requirements verified:

1. HOST-02: `grep -q '"0\.0\.0\.0"' tools/hlx-admin-mcp/src/http.ts` — passes
2. HOST-02: `grep -q 'process\.env\.PORT' tools/hlx-admin-mcp/src/http.ts` — passes
3. HOST-03: `node -e "const p=require('./tools/hlx-admin-mcp/package.json'); process.exit(p.dependencies['selfsigned'] ? 1 : 0)"` — exits 0
4. HOST-04: Start server with `PORT=9999 node tools/hlx-admin-mcp/dist/http.js &` then `curl -sf http://localhost:9999/health` — returns 200 JSON
5. HOST-05: `test -f tools/hlx-admin-mcp/railway.toml && grep -q 'healthcheckPath' tools/hlx-admin-mcp/railway.toml` — passes
</verification>

<success_criteria>
- selfsigned removed from package.json and package-lock.json
- npm run build exits 0 on clean install (no selfsigned import errors)
- test-phase1.sh exits 0 — all 10 checks green
- Phase 1 success criteria from ROADMAP.md are all satisfied:
  1. Server starts when PORT env var is set and binds to 0.0.0.0 ✓
  2. One Express app, one port — no dual-server, no selfsigned ✓
  3. GET /health returns HTTP 200 with JSON body ✓
  4. railway.toml exists with build/start command and /health health check path ✓
</success_criteria>

<output>
After completion, create `c:/Users/remekie/Documents/Antigravity/.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
